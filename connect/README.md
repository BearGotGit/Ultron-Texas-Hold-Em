# Directions

0. Start go server with `go run .` in that project's root

1. Ready >=2 bots over here

    - Look at `.env` and go needs to expect right table. Ctrl+f for `table-` and find two places.
    - For players `python3 -m connect.player1 bpg2 simple`.

2. Run `python3 -m connect.start_game` to play one game to completion.
